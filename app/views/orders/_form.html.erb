<%= form_for(@order) do |f| %>
  <%= render 'shared/error_messages', form_object: f.object %>

  <%= f.label :billing_name %>
  <%= f.text_field :billing_name, class: 'form-control' %>

  <%= f.label :billing_name %>
  <%= f.text_field :billing_name, class: 'form-control' %>

  <%= f.submit "Submit Order Info", class: "btn btn-primary" %>
<% end %>
