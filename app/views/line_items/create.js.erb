$(".alert").hide("slow");

$(".cart-quantity").html(" <%= pluralize(@cart.item_quantity, "item") %>");

$(window).off()

$(window).on('popstate', function(event) {
  $(".cart-quantity").html(" <%= pluralize(@cart.item_quantity, "item") %> ");
  console.log("<%= @cart.item_quantity %>");
});
